# Dev env for the @qp/web workspace. Loaded by the compose web service
# via env_file. Vite / Nitro consume NODE_ENV directly; future
# environment-driven config (public API URL, public Stripe key, …)
# lands here.

NODE_ENV=development
